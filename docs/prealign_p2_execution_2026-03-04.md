# 预对齐 P2 执行记录

日期: 2026-03-04

## 已完成

1. 区域候选生成
- 新增 `suggest_region_candidates(left_page, right_page, top_k)`。
- 优先基于共享锚点生成左右区域框；锚点不足时提供兜底候选框。

2. GUI 候选窗口
- 顶部新增按钮 `预对齐建议`。
- 点击后弹窗展示候选列表（页号、分数、原因、失败分类）。
- 用户可选择并点击 `应用候选` 一键填充左右选区，再直接点击比对或继续微调。

3. 自动化测试
- `tests/test_prealign_service.py` 增加区域候选测试。
- 全量测试通过。

## 现状说明

- 机器预对齐现在会给用户一个“可检查正确性”的窗口，但仍是 **建议式**，不是强制自动提交。
- 这符合当前人机协同策略：机器先给候选，用户确认/微调后再比对。

